# Exclude heavy local ONNX transformer embedding model auto-configurations in production to avoid OOM / duplicate bean conflicts.
spring.autoconfigure.exclude=org.springframework.ai.autoconfigure.transformers.TransformersEmbeddingModelAutoConfiguration,\
  org.springframework.ai.model.transformers.autoconfigure.TransformersEmbeddingModelAutoConfiguration

# Production Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://workmatedbmysql-contest037-8f7d.l.aivencloud.com:27604/defaultdb?sslMode=REQUIRED}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
